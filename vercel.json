{
    "framework": "hugo",
    "buildCommand": "bash ./build.sh",
    "outputDirectory": "public",
    "installCommand": "wget -O hugo.deb https://github.com/gohugoio/hugo/releases/download/v0.128.0/hugo_extended_0.128.0_linux-amd64.deb && sudo dpkg -i hugo.deb && npm install",
    "builds": [
        {
            "src": "**",
            "use": "@vercel/static"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/$1"
        }
    ]
}